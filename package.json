{
  "name": "world-cup-predictor",
  "version": "1.0.0",
  "description": "World Cup score prediction app with auth, persistent user data, live match refresh, and leaderboard.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "reset-data": "node scripts/reset-data.js",
    "setup": "npm install && node -e \"const fs=require('fs'); if(!fs.existsSync('.env')) fs.copyFileSync('.env.example','.env'); console.log('Setup complete. Run npm start.');\"",
    "check": "node scripts/check-ready.js"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-session": "^1.18.1"
  },
  "engines": {
    "node": ">=18"
  }
}
