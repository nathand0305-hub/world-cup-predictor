# תתחיל כאן — מדריך הרצה בעברית

הפרויקט הזה כבר מוכן להרצה מקומית על המחשב שלך.

## מה יש בפנים

- אתר מונדיאל לניחושי תוצאות
- הרשמה עם שם, שם משתמש וסיסמה
- התחברות חוזרת עם סיסמה
- שמירת משתמשים, ניחושים ונקודות
- נעילת ניחוש כשהמשחק מתחיל
- חישוב נקודות אוטומטי בשרת
- לוח מובילים
- דאשבורד אישי לכל משתמש
- משחקי דמו מוכנים לבדיקה

---

## הדבר היחיד שאתה חייב שיהיה מותקן

צריך שיהיה מותקן במחשב שלך `Node.js`.

כדי לבדוק אם יש לך:

### Mac
פתח Terminal וכתוב:

```bash
node -v
```

### Windows
פתח PowerShell וכתוב:

```powershell
node -v
```

אם מופיע משהו כמו:

```txt
v18.0.0
```

או מספר גבוה יותר — אתה מסודר.

אם כתוב שהפקודה לא קיימת, תתקין `Node.js` מהאתר הרשמי של Node.js, ואז תסגור ותפתח מחדש את ה-Terminal / PowerShell.

---

## הפעלה הכי קלה — Windows

1. חלץ את קובץ ה־ZIP.
2. פתח את התיקייה:

```txt
world-cup-predictor
```

3. תלחץ פעמיים על הקובץ:

```txt
start-windows.bat
```

4. כשהחלון נפתח, אל תסגור אותו.
5. פתח בדפדפן:

```txt
http://localhost:3000
```

---

## הפעלה הכי קלה — Mac

1. חלץ את קובץ ה־ZIP.
2. פתח Terminal.
3. גרור את התיקייה `world-cup-predictor` לתוך ה-Terminal אחרי הפקודה `cd`.

זה ייראה בערך ככה:

```bash
cd /Users/yourname/Downloads/world-cup-predictor
```

4. לחץ Enter.
5. כתוב:

```bash
chmod +x start-mac-linux.sh
./start-mac-linux.sh
```

6. אל תסגור את ה-Terminal.
7. פתח בדפדפן:

```txt
http://localhost:3000
```

---

## מה לעשות באתר

1. לחץ `Join / Login`.
2. צור משתמש חדש.
3. עבור ל־`Matches`.
4. שים ניחוש תוצאה לכל משחק.
5. עבור ל־`Dashboard` כדי לראות את הניחושים שלך.
6. עבור ל־`Leaderboard` כדי לראות דירוג.

---

## ניקוד

- תוצאה מדויקת: `5` נקודות
- מנצחת נכונה אבל תוצאה לא מדויקת: `2` נקודות
- תיקו נכון אבל תוצאה לא מדויקת: `2` נקודות
- טעות: `0` נקודות

הנקודות רק עולות, לא יורדות.

---

## איפה הנתונים נשמרים

הכול נשמר בקובץ:

```txt
data/data.json
```

שם נשמרים:

- משתמשים
- סיסמאות מוצפנות
- ניחושים
- נקודות
- משחקים

---

## איך לבדוק שהניקוד עובד

1. פתח את האתר.
2. צור משתמש.
3. נחש תוצאה במשחק דמו.
4. עצור את השרת עם:

```txt
Ctrl + C
```

5. פתח את הקובץ:

```txt
data/data.json
```

6. שנה משחק ל־`finished` ושם לו תוצאה אמיתית.

לדוגמה:

```json
"status": "finished",
"homeScore": 2,
"awayScore": 1
```

7. שמור את הקובץ.
8. תריץ שוב את האתר.
9. תרענן את הדפדפן.

השרת יחשב נקודות לבד.

---

## אם משהו לא עובד

### הבעיה: כתוב ש־Node לא מותקן

תתקין `Node.js`, ואז תפתח מחדש את Terminal / PowerShell.

### הבעיה: הפורט 3000 תפוס

יכול להיות שהאתר כבר רץ בחלון אחר.

אפשר לפתוח את הקובץ `.env` ולשנות:

```env
PORT=3000
```

למשל:

```env
PORT=3001
```

ואז לפתוח:

```txt
http://localhost:3001
```

### הבעיה: סגרת את החלון השחור

זה עוצר את האתר. פשוט תפעיל שוב את קובץ ההפעלה.

---

## חיבור למשחקים אמיתיים של המונדיאל

כרגע יש משחקי דמו כדי שהאתר יעבוד ישר.

כדי לחבר API אמיתי, פותחים את `.env` וממלאים:

```env
FOOTBALL_API_URL=https://your-football-api-url-here
FOOTBALL_API_KEY=your-api-key-here
FOOTBALL_API_HOST=optional-host-if-needed
MATCH_SYNC_INTERVAL_MS=60000
```

בלי API האתר עדיין עובד עם משחקי הדמו.
